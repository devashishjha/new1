# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 10
environmentVariables:
  # Secret for Genkit AI features.
  GOOGLE_API_KEY:
    secret: GOOGLE_API_KEY
  # Secret for Firebase client-side API key.
  NEXT_PUBLIC_FIREBASE_API_KEY:
    secret: NEXT_PUBLIC_FIREBASE_API_KEY
  # Secret for Firebase client-side auth domain.
  NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN:
    secret: NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN
  # Secret for Firebase client-side project ID.
  NEXT_PUBLIC_FIREBASE_PROJECT_ID:
    secret: NEXT_PUBLIC_FIREBASE_PROJECT_ID
  # Secret for Firebase client-side storage bucket.
  NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET:
    secret: NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET
  # Secret for Firebase client-side messaging sender ID.
  NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID:
    secret: NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID
  # Secret for Firebase client-side app ID.
  NEXT_PUBLIC_FIREBASE_APP_ID:
    secret: NEXT_PUBLIC_FIREBASE_APP_ID
